Approach #1 Brute Force [Time Limit Exceeded]
从每个字母开始遍历至最后，分别查找allunique的最长子字符串个数

Approach #2 Sliding Window [Accepted]
从每个字母开始，使用HashSet的滑窗，逐渐增加窗口右边坐标直到发现重复字母，这时就可以得到最长子序列。对每个字母都使用这样的滑窗找到最长子序列的长度。
对于每一个j，先判断，如果不重复就加入这个字母并且j++，否则删除i并且i++。

Approach #3 Sliding Window Optimized [Accepted]
对于每一个滑窗右界限j，如果重复了字母m，更新i为m在除j位置之前出现的最后一次位置。然后更新最长子序列长度，和map中此重复字母的位置（更新为新出现的位置）
即如果s[j]在[i,j)中有重复，重复的位置为j'，我们不需要一点点增加i，可以跳过[i,j]部分直接把i变为j'+1
